
/* 
 * File:   tablas.c
 * Author: Usuario
 *
 * Created on 11 de noviembre de 2020, 09:44 PM
 */

#include <stdio.h>
#include <stdlib.h>

/*
 * 
 */
int main(int argc, char** argv) {

    int x = 1; 
    int t = 1;
    int tope = 10;
    
    while (x<=tope) {
        printf("%d X %d = %d\n",t,x,t*x);
        x = x + 1;
    } 
    if (x = 1)
        if (t = 2)
         while (x<=tope) {
             printf("%d X %d = %d\n",t,x,t*x);
            x = x + 1;
         }
    if (x = 1)
        if (t = 3)
            while  (x<=tope) {
             printf("%d X %d = %d\n",t,x,t*x);
            x = x + 1;
         }
        
        
    if (x = 1)
        if (t = 4)
            while  (x<=tope) {
             printf("%d X %d = %d\n",t,x,t*x);
            x = x + 1;
         }
    
    if (x = 1)
        if (t = 5)
            while  (x<=tope) {
             printf("%d X %d = %d\n",t,x,t*x);
            x = x + 1;
         }
    
    if (x = 1)
        if (t = 6)
            while  (x<=tope) {
             printf("%d X %d = %d\n",t,x,t*x);
            x = x + 1;
         }
    
    
    if (x = 1)
        if (t = 7)
            while  (x<=tope) {
             printf("%d X %d = %d\n",t,x,t*x);
            x = x + 1;
         }
    
    
    if (x = 1)
        if (t = 8)
            while  (x<=tope) {
             printf("%d X %d = %d\n",t,x,t*x);
            x = x + 1;
         }
    
    if (x = 1)
        if (t = 9)
            while  (x<=tope) {
             printf("%d X %d = %d\n",t,x,t*x);
            x = x + 1;
         }
    
    if (x = 1)
        if (t = 10)
            while  (x<=tope) {
             printf("%d X %d = %d\n",t,x,t*x);
            x = x + 1;
         }
    
    return 0;
    
    
    
    
    

}







